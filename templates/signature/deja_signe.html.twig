<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Déjà signé - CFA</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }

        .card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            max-width: 500px;
            width: 100%;
            overflow: hidden;
            text-align: center;
        }

        .card__header {
            background: linear-gradient(135deg, #2196f3, #1976d2);
            color: white;
            padding: 2.5rem 2rem;
        }

        .info-icon {
            width: 80px;
            height: 80px;
            background: rgba(255,255,255,0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.25rem;
            font-size: 2.5rem;
        }

        .card__title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .card__subtitle {
            opacity: 0.9;
            font-size: 0.95rem;
        }

        .card__body {
            padding: 2rem;
        }

        .confirmation-details {
            background: #e3f2fd;
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            border: 1px solid #90caf9;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 0.6rem 0;
            border-bottom: 1px solid #bbdefb;
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .detail-row__label {
            color: #1565c0;
            font-size: 0.9rem;
        }

        .detail-row__value {
            font-weight: 600;
            color: #0d47a1;
        }

        .timestamp {
            background: #e8f5e9;
            color: #2e7d32;
            padding: 1rem;
            border-radius: 8px;
            font-size: 0.9rem;
            margin-bottom: 1.5rem;
            border: 1px solid #a5d6a7;
        }

        .timestamp strong {
            display: block;
            font-size: 1rem;
            margin-bottom: 0.25rem;
        }

        .message {
            color: #666;
            font-size: 0.95rem;
            line-height: 1.6;
        }

        .footer {
            padding: 1rem 2rem 2rem;
            font-size: 0.8rem;
            color: #999;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="card__header">
            <div class="info-icon">ℹ️</div>
            <h1 class="card__title">Déjà signé !</h1>
            <p class="card__subtitle">Votre présence est déjà enregistrée</p>
        </div>

        <div class="card__body">
            <div class="confirmation-details">
                <div class="detail-row">
                    <span class="detail-row__label">Cours</span>
                    <span class="detail-row__value">
                        {% if seance.sessionMatiere and seance.sessionMatiere.matiere %}
                            {{ seance.sessionMatiere.matiere.libelle }}
                        {% else %}
                            Cours
                        {% endif %}
                    </span>
                </div>
                <div class="detail-row">
                    <span class="detail-row__label">Date</span>
                    <span class="detail-row__value">{{ seance.date|date('d/m/Y') }}</span>
                </div>
                <div class="detail-row">
                    <span class="detail-row__label">Horaires</span>
                    <span class="detail-row__value">{{ seance.heureDebut|date('H:i') }} - {{ seance.heureFin|date('H:i') }}</span>
                </div>
            </div>

            {% if presence.dateSignature %}
            <div class="timestamp">
                <strong>✅ Signature enregistrée le</strong>
                {{ presence.dateSignature|date('d/m/Y à H:i:s') }}
            </div>
            {% endif %}

            <p class="message">
                Bonjour {{ apprenti.prenom }}, vous avez déjà signé votre présence pour ce cours.<br>
                Aucune action supplémentaire n'est nécessaire.
            </p>
        </div>

        <div class="footer">
            CFA Gestion - Système de signature électronique de présence
        </div>
    </div>
</body>
</html>
