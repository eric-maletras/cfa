{% extends 'base.html.twig' %}

{% block title %}{{ module.nom }}{% endblock %}

{% block body %}
<section class="dashboard">
    <div class="container">
        <header class="dashboard__header">
            <h1 class="dashboard__title">🚧 {{ module.nom }}</h1>
            <p class="dashboard__subtitle">Cette fonctionnalité est en cours de développement.</p>
        </header>
        <div class="dashboard__empty">
            <p>{{ module.description }}</p>
            <a href="{{ path('app_dashboard') }}" class="btn btn--primary">Retour au tableau de bord</a>
        </div>
    </div>
</section>
{% endblock %}
