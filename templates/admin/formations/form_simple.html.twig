{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}

{% block body %}
<section class="dashboard">
    <div class="container container--narrow">
        <header class="dashboard__header">
            <div class="header-actions">
                <a href="{{ path('admin_formation_index', {tab: backTab}) }}" class="btn btn--secondary btn--sm">
                    ← Retour à la liste
                </a>
            </div>
            <h1 class="dashboard__title">{{ title }}</h1>
        </header>
        
        <div class="form-container form-container--compact">
            {{ form_start(form, {'attr': {'class': 'admin-form'}}) }}
            
            <div class="form-section">
                {% for field in form %}
                    {% if field.vars.name not in ['_token'] %}
                        <div class="form-group">
                            {{ form_row(field) }}
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
            
            <div class="form-actions">
                <a href="{{ path('admin_formation_index', {tab: backTab}) }}" class="btn btn--secondary">
                    Annuler
                </a>
                <button type="submit" class="btn btn--primary">
                    💾 Enregistrer
                </button>
            </div>
            
            {{ form_end(form) }}
        </div>
    </div>
</section>
{% endblock %}
