{% extends 'base.html.twig' %}

{% block title %}Gestion des formations{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}

{% block body %}
<section class="dashboard">
    <div class="container">
        {# En-tête #}
        <header class="dashboard__header">
            <div class="header-actions">
                <a href="{{ path('app_dashboard') }}" class="btn btn--secondary btn--sm">
                    ← Retour au tableau de bord
                </a>
            </div>
            <h1 class="dashboard__title">📚 Gestion des formations</h1>
            <p class="dashboard__subtitle">Gérer les formations et les tables de référence</p>
        </header>
        
        {# Onglets de navigation #}
        <div class="tabs">
            <nav class="tabs__nav">
                <a href="{{ path('admin_formation_index', {tab: 'formations'}) }}" 
                   class="tabs__link {{ activeTab == 'formations' ? 'tabs__link--active' : '' }}">
                    🎓 Formations
                    <span class="badge">{{ formations|length }}</span>
                </a>
                <a href="{{ path('admin_formation_index', {tab: 'niveaux'}) }}" 
                   class="tabs__link {{ activeTab == 'niveaux' ? 'tabs__link--active' : '' }}">
                    📊 Niveaux
                    <span class="badge">{{ niveaux|length }}</span>
                </a>
                <a href="{{ path('admin_formation_index', {tab: 'types'}) }}" 
                   class="tabs__link {{ activeTab == 'types' ? 'tabs__link--active' : '' }}">
                    📋 Types
                    <span class="badge">{{ types|length }}</span>
                </a>
                <a href="{{ path('admin_formation_index', {tab: 'nsf'}) }}" 
                   class="tabs__link {{ activeTab == 'nsf' ? 'tabs__link--active' : '' }}">
                    🏷️ Codes NSF
                    <span class="badge">{{ codesNsf|length }}</span>
                </a>
                <a href="{{ path('admin_formation_index', {tab: 'rome'}) }}" 
                   class="tabs__link {{ activeTab == 'rome' ? 'tabs__link--active' : '' }}">
                    💼 Codes ROME
                    <span class="badge">{{ codesRome|length }}</span>
                </a>
            </nav>
            
            {# Contenu des onglets #}
            <div class="tabs__content">
                
                {# ============================================
                   ONGLET FORMATIONS
                   ============================================ #}
                {% if activeTab == 'formations' %}
                <div class="tab-pane">
                    <div class="tab-pane__header">
                        <h2>Formations proposées par le CFA</h2>
                        <a href="{{ path('admin_formation_new') }}" class="btn btn--primary">
                            + Nouvelle formation
                        </a>
                    </div>
                    
                    {% if formations is empty %}
                        <div class="empty-state">
                            <p>Aucune formation n'a encore été créée.</p>
                            <a href="{{ path('admin_formation_new') }}" class="btn btn--primary">
                                Créer la première formation
                            </a>
                        </div>
                    {% else %}
                        <div class="table-responsive">
                            <table class="data-table">
                                <thead>
                                    <tr>
                                        <th>Intitulé</th>
                                        <th>Code RNCP</th>
                                        <th>Type</th>
                                        <th>Niveau</th>
                                        <th>Durée</th>
                                        <th>Statut</th>
                                        <th class="actions-col">Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for formation in formations %}
                                    <tr class="{{ not formation.actif ? 'row--inactive' : '' }}">
                                        <td>
                                            <strong>{{ formation.intituleCourt ?? formation.intitule }}</strong>
                                            {% if formation.intituleCourt %}
                                                <br><small class="text-muted">{{ formation.intitule }}</small>
                                            {% endif %}
                                        </td>
                                        <td>{{ formation.codeRncp ?? '—' }}</td>
                                        <td>{{ formation.typeCertification.libelleAbrege ?? formation.typeCertification }}</td>
                                        <td>{{ formation.niveauQualification.code ?? '—' }}</td>
                                        <td>{{ formation.dureeMois ? formation.dureeMois ~ ' mois' : '—' }}</td>
                                        <td>
                                            {% if formation.actif %}
                                                <span class="status status--success">Actif</span>
                                            {% else %}
                                                <span class="status status--danger">Inactif</span>
                                            {% endif %}
                                        </td>
                                        <td class="actions-col">
                                            <div class="btn-group">
                                                <a href="{{ path('admin_formation_edit', {id: formation.id}) }}" 
                                                   class="btn btn--sm btn--secondary" title="Modifier">
                                                    ✏️
                                                </a>
                                                <form action="{{ path('admin_formation_toggle', {id: formation.id}) }}" 
                                                      method="post" style="display:inline;">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('toggle' ~ formation.id) }}">
                                                    <button type="submit" class="btn btn--sm btn--outline" 
                                                            title="{{ formation.actif ? 'Désactiver' : 'Activer' }}">
                                                        {{ formation.actif ? '🔒' : '🔓' }}
                                                    </button>
                                                </form>
                                                <form action="{{ path('admin_formation_delete', {id: formation.id}) }}" 
                                                      method="post" style="display:inline;"
                                                      onsubmit="return confirm('Supprimer cette formation ?');">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.id) }}">
                                                    <button type="submit" class="btn btn--sm btn--danger" title="Supprimer">
                                                        🗑️
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    {% endif %}
                </div>
                {% endif %}
                
                {# ============================================
                   ONGLET NIVEAUX DE QUALIFICATION
                   ============================================ #}
                {% if activeTab == 'niveaux' %}
                <div class="tab-pane">
                    <div class="tab-pane__header">
                        <h2>Niveaux de qualification (RNCP)</h2>
                        <a href="{{ path('admin_niveau_new') }}" class="btn btn--primary">
                            + Nouveau niveau
                        </a>
                    </div>
                    
                    <div class="table-responsive">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Niveau</th>
                                    <th>Libellé</th>
                                    <th>Équivalent diplôme</th>
                                    <th>Ancien (1969)</th>
                                    <th>CEC</th>
                                    <th>Formations</th>
                                    <th>Statut</th>
                                    <th class="actions-col">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for niveau in niveaux %}
                                <tr class="{{ not niveau.actif ? 'row--inactive' : '' }}">
                                    <td><strong>{{ niveau.code }}</strong></td>
                                    <td>{{ niveau.libelle }}</td>
                                    <td>{{ niveau.equivalentDiplome }}</td>
                                    <td>{{ niveau.ancienNiveau ?? '—' }}</td>
                                    <td>{{ niveau.niveauCec ?? '—' }}</td>
                                    <td>
                                        <span class="badge badge--info">{{ niveau.formations|length }}</span>
                                    </td>
                                    <td>
                                        {% if niveau.actif %}
                                            <span class="status status--success">Actif</span>
                                        {% else %}
                                            <span class="status status--danger">Inactif</span>
                                        {% endif %}
                                    </td>
                                    <td class="actions-col">
                                        <div class="btn-group">
                                            <a href="{{ path('admin_niveau_edit', {id: niveau.id}) }}" 
                                               class="btn btn--sm btn--secondary">✏️</a>
                                            {% if niveau.formations|length == 0 %}
                                            <form action="{{ path('admin_niveau_delete', {id: niveau.id}) }}" 
                                                  method="post" style="display:inline;"
                                                  onsubmit="return confirm('Supprimer ce niveau ?');">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ niveau.id) }}">
                                                <button type="submit" class="btn btn--sm btn--danger">🗑️</button>
                                            </form>
                                            {% endif %}
                                        </div>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
                {% endif %}
                
                {# ============================================
                   ONGLET TYPES DE CERTIFICATION
                   ============================================ #}
                {% if activeTab == 'types' %}
                <div class="tab-pane">
                    <div class="tab-pane__header">
                        <h2>Types de certification</h2>
                        <a href="{{ path('admin_type_new') }}" class="btn btn--primary">
                            + Nouveau type
                        </a>
                    </div>
                    
                    <div class="table-responsive">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Code</th>
                                    <th>Libellé</th>
                                    <th>Certificateur</th>
                                    <th>RNCP</th>
                                    <th>Apprentissage</th>
                                    <th>VAE</th>
                                    <th>Formations</th>
                                    <th class="actions-col">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for type in types %}
                                <tr class="{{ not type.actif ? 'row--inactive' : '' }}">
                                    <td><strong>{{ type.code }}</strong></td>
                                    <td>
                                        {{ type.libelleAbrege ?? type.libelle }}
                                        {% if type.libelleAbrege %}
                                            <br><small class="text-muted">{{ type.libelle }}</small>
                                        {% endif %}
                                    </td>
                                    <td>{{ type.certificateurType|capitalize }}</td>
                                    <td>{{ type.enregistrementRncp|replace({'_': ' '})|capitalize }}</td>
                                    <td>{{ type.apprentissagePossible ? '✅' : '❌' }}</td>
                                    <td>{{ type.vaePossible ? '✅' : '❌' }}</td>
                                    <td>
                                        <span class="badge badge--info">{{ type.formations|length }}</span>
                                    </td>
                                    <td class="actions-col">
                                        <div class="btn-group">
                                            <a href="{{ path('admin_type_edit', {id: type.id}) }}" 
                                               class="btn btn--sm btn--secondary">✏️</a>
                                            {% if type.formations|length == 0 %}
                                            <form action="{{ path('admin_type_delete', {id: type.id}) }}" 
                                                  method="post" style="display:inline;"
                                                  onsubmit="return confirm('Supprimer ce type ?');">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
                                                <button type="submit" class="btn btn--sm btn--danger">🗑️</button>
                                            </form>
                                            {% endif %}
                                        </div>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
                {% endif %}
                
                {# ============================================
                   ONGLET CODES NSF
                   ============================================ #}
                {% if activeTab == 'nsf' %}
                <div class="tab-pane">
                    <div class="tab-pane__header">
                        <h2>Codes NSF (Nomenclature des Spécialités de Formation)</h2>
                        <a href="{{ path('admin_nsf_new') }}" class="btn btn--primary">
                            + Nouveau code NSF
                        </a>
                    </div>
                    
                    <div class="table-responsive">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Code</th>
                                    <th>Libellé</th>
                                    <th>Niveau</th>
                                    <th>Parent</th>
                                    <th>Formations</th>
                                    <th>Statut</th>
                                    <th class="actions-col">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for nsf in codesNsf %}
                                <tr class="{{ not nsf.actif ? 'row--inactive' : '' }}">
                                    <td><strong>{{ nsf.code }}</strong></td>
                                    <td>{{ nsf.libelle }}</td>
                                    <td>{{ nsf.niveau }}</td>
                                    <td>{{ nsf.parent ? nsf.parent.code : '—' }}</td>
                                    <td>
                                        <span class="badge badge--info">{{ nsf.formations|length }}</span>
                                    </td>
                                    <td>
                                        {% if nsf.actif %}
                                            <span class="status status--success">Actif</span>
                                        {% else %}
                                            <span class="status status--danger">Inactif</span>
                                        {% endif %}
                                    </td>
                                    <td class="actions-col">
                                        <div class="btn-group">
                                            <a href="{{ path('admin_nsf_edit', {id: nsf.id}) }}" 
                                               class="btn btn--sm btn--secondary">✏️</a>
                                            {% if nsf.formations|length == 0 %}
                                            <form action="{{ path('admin_nsf_delete', {id: nsf.id}) }}" 
                                                  method="post" style="display:inline;"
                                                  onsubmit="return confirm('Supprimer ce code NSF ?');">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nsf.id) }}">
                                                <button type="submit" class="btn btn--sm btn--danger">🗑️</button>
                                            </form>
                                            {% endif %}
                                        </div>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
                {% endif %}
                
                {# ============================================
                   ONGLET CODES ROME
                   ============================================ #}
                {% if activeTab == 'rome' %}
                <div class="tab-pane">
                    <div class="tab-pane__header">
                        <h2>Codes ROME (Répertoire Opérationnel des Métiers)</h2>
                        <a href="{{ path('admin_rome_new') }}" class="btn btn--primary">
                            + Nouveau code ROME
                        </a>
                    </div>
                    
                    <div class="table-responsive">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Code</th>
                                    <th>Métier</th>
                                    <th>Domaine</th>
                                    <th>Formations</th>
                                    <th>Statut</th>
                                    <th class="actions-col">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for rome in codesRome %}
                                <tr class="{{ not rome.actif ? 'row--inactive' : '' }}">
                                    <td><strong>{{ rome.code }}</strong></td>
                                    <td>{{ rome.libelle }}</td>
                                    <td>
                                        <span class="badge badge--secondary">{{ rome.domaineCode }}</span>
                                        {{ rome.domaineLibelle ?? '' }}
                                    </td>
                                    <td>
                                        <span class="badge badge--info">{{ rome.formations|length }}</span>
                                    </td>
                                    <td>
                                        {% if rome.actif %}
                                            <span class="status status--success">Actif</span>
                                        {% else %}
                                            <span class="status status--danger">Inactif</span>
                                        {% endif %}
                                    </td>
                                    <td class="actions-col">
                                        <div class="btn-group">
                                            <a href="{{ path('admin_rome_edit', {id: rome.id}) }}" 
                                               class="btn btn--sm btn--secondary">✏️</a>
                                            {% if rome.formations|length == 0 %}
                                            <form action="{{ path('admin_rome_delete', {id: rome.id}) }}" 
                                                  method="post" style="display:inline;"
                                                  onsubmit="return confirm('Supprimer ce code ROME ?');">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rome.id) }}">
                                                <button type="submit" class="btn btn--sm btn--danger">🗑️</button>
                                            </form>
                                            {% endif %}
                                        </div>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
                {% endif %}
                
            </div>
        </div>
    </div>
</section>
{% endblock %}
