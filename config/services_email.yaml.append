# Ajouter à config/services.yaml dans la section services:

    # Service Email avec paramètres configurables
    App\Service\EmailService:
        arguments:
            $fromAddress: '%env(MAILER_FROM_ADDRESS)%'
            $fromName: '%env(MAILER_FROM_NAME)%'
